ebean:
  test:
    useDocker: false # we do not use docker for testing
    platform: postgres #, h2, postgres, mysql, oracle, sqlserver, hana, clickhouse, sqlite
    ddlMode: dropCreate # none | dropCreate | migrations | create
    dbName: ibi
    postgres:
      username: zen
      password: zen
      url: jdbc:postgresql://192.168.99.100:65432/ibi

